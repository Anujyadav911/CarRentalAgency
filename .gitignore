# ── Sensitive config (DB credentials) ─────────────────────────────────────────
config/db.php

# ── PHP logs & cache ───────────────────────────────────────────────────────────
*.log
error_log
*.cache

# ── OS files ───────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── IDE / Editor ───────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo

# ── Composer (if added later) ──────────────────────────────────────────────────
vendor/
composer.lock
